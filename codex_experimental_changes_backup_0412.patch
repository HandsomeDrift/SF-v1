diff --git a/diffusion_video_brain.py b/diffusion_video_brain.py
index c8ea40f..4836271 100644
--- a/diffusion_video_brain.py
+++ b/diffusion_video_brain.py
@@ -33,6 +33,7 @@ class SATVideoDiffusionEngineBrain(nn.Module):
         self._freeze_fast = model_config.get('freeze_fast_branch', False)
         self._unfreeze_fusion = model_config.get('unfreeze_fusion', False)
         self._reset_gate_net = model_config.get('reset_gate_net', False)
+        self._reset_fusion_guidance_full = model_config.get('reset_fusion_guidance_full', False)
         # model args preprocess
         log_keys = model_config.get("log_keys", None)
         input_key = model_config.get("input_key", "mp4")
@@ -185,17 +186,28 @@ class SATVideoDiffusionEngineBrain(nn.Module):
                     fusion_unfrozen += p.numel()
             print_rank0(f"[Fusion] Unfroze fusion modules: {fusion_unfrozen:,} params")
 
-        # Stage 3: reset gate_net + guidance cross-attn out_proj
-        # Avoids inheriting saturated sigmoid values (gate_net) and noisy
-        # cross-attn output (out_proj) from Stage 2 where alpha was 0.
-        # After reset: alpha=0.5, cross_attn_output=0 → neutral start,
-        # cross-attn learns through non-zero alpha before alpha adapts.
+        # Stage 2.5/3: reset fusion/guidance after checkpoint load.
         _reset_gate_net = getattr(self, '_reset_gate_net', False)
-        if _reset_gate_net:
+        _reset_fusion_guidance_full = getattr(self, '_reset_fusion_guidance_full', False)
+        if _reset_fusion_guidance_full:
+            for emb in self.conditioner.embedders:
+                if hasattr(emb, 'gated_fusion') and hasattr(emb.gated_fusion, 'reset_fusion_state'):
+                    emb.gated_fusion.reset_fusion_state(full=True)
+                    print_rank0("[GatedFusion] Full reset → projections, fusion stack, and gate head reinitialized")
+                elif hasattr(emb, 'gated_fusion') and hasattr(emb.gated_fusion, 'reset_gate_net'):
+                    emb.gated_fusion.reset_gate_net()
+                    print_rank0("[GatedFusion] Fallback reset → gate head reinitialized")
+                if hasattr(emb, 'guidance_adapter') and hasattr(emb.guidance_adapter, 'reset_guidance_state'):
+                    emb.guidance_adapter.reset_guidance_state(full=True)
+                    print_rank0("[MultiGuidance] Full reset → projections and cross-attn layers reinitialized")
+                elif hasattr(emb, 'guidance_adapter') and hasattr(emb.guidance_adapter, 'reset_guidance_outputs'):
+                    emb.guidance_adapter.reset_guidance_outputs()
+                    print_rank0("[MultiGuidance] Fallback reset → cross-attn output projections zeroed")
+        elif _reset_gate_net:
             for emb in self.conditioner.embedders:
                 if hasattr(emb, 'gated_fusion') and hasattr(emb.gated_fusion, 'reset_gate_net'):
                     emb.gated_fusion.reset_gate_net()
-                    print_rank0("[GatedFusion] Reset gate_net → sigmoid(0)=0.5 for all alphas")
+                    print_rank0("[GatedFusion] Reset gate head → uniform alpha prior")
                 if hasattr(emb, 'guidance_adapter') and hasattr(emb.guidance_adapter, 'reset_guidance_outputs'):
                     emb.guidance_adapter.reset_guidance_outputs()
                     print_rank0("[MultiGuidance] Reset cross-attn out_proj → output=0 (neutral)")
@@ -489,6 +501,7 @@ class SATVideoDiffusionEngineBrain_fix(nn.Module):
         self._freeze_fast = model_config.get('freeze_fast_branch', False)
         self._unfreeze_fusion = model_config.get('unfreeze_fusion', False)
         self._reset_gate_net = model_config.get('reset_gate_net', False)
+        self._reset_fusion_guidance_full = model_config.get('reset_fusion_guidance_full', False)
         # model args preprocess
         log_keys = model_config.get("log_keys", None)
         input_key = model_config.get("input_key", "mp4")
@@ -641,17 +654,28 @@ class SATVideoDiffusionEngineBrain_fix(nn.Module):
                     fusion_unfrozen += p.numel()
             print_rank0(f"[Fusion] Unfroze fusion modules: {fusion_unfrozen:,} params")
 
-        # Stage 3: reset gate_net + guidance cross-attn out_proj
-        # Avoids inheriting saturated sigmoid values (gate_net) and noisy
-        # cross-attn output (out_proj) from Stage 2 where alpha was 0.
-        # After reset: alpha=0.5, cross_attn_output=0 → neutral start,
-        # cross-attn learns through non-zero alpha before alpha adapts.
+        # Stage 2.5/3: reset fusion/guidance after checkpoint load.
         _reset_gate_net = getattr(self, '_reset_gate_net', False)
-        if _reset_gate_net:
+        _reset_fusion_guidance_full = getattr(self, '_reset_fusion_guidance_full', False)
+        if _reset_fusion_guidance_full:
+            for emb in self.conditioner.embedders:
+                if hasattr(emb, 'gated_fusion') and hasattr(emb.gated_fusion, 'reset_fusion_state'):
+                    emb.gated_fusion.reset_fusion_state(full=True)
+                    print_rank0("[GatedFusion] Full reset → projections, fusion stack, and gate head reinitialized")
+                elif hasattr(emb, 'gated_fusion') and hasattr(emb.gated_fusion, 'reset_gate_net'):
+                    emb.gated_fusion.reset_gate_net()
+                    print_rank0("[GatedFusion] Fallback reset → gate head reinitialized")
+                if hasattr(emb, 'guidance_adapter') and hasattr(emb.guidance_adapter, 'reset_guidance_state'):
+                    emb.guidance_adapter.reset_guidance_state(full=True)
+                    print_rank0("[MultiGuidance] Full reset → projections and cross-attn layers reinitialized")
+                elif hasattr(emb, 'guidance_adapter') and hasattr(emb.guidance_adapter, 'reset_guidance_outputs'):
+                    emb.guidance_adapter.reset_guidance_outputs()
+                    print_rank0("[MultiGuidance] Fallback reset → cross-attn output projections zeroed")
+        elif _reset_gate_net:
             for emb in self.conditioner.embedders:
                 if hasattr(emb, 'gated_fusion') and hasattr(emb.gated_fusion, 'reset_gate_net'):
                     emb.gated_fusion.reset_gate_net()
-                    print_rank0("[GatedFusion] Reset gate_net → sigmoid(0)=0.5 for all alphas")
+                    print_rank0("[GatedFusion] Reset gate head → uniform alpha prior")
                 if hasattr(emb, 'guidance_adapter') and hasattr(emb.guidance_adapter, 'reset_guidance_outputs'):
                     emb.guidance_adapter.reset_guidance_outputs()
                     print_rank0("[MultiGuidance] Reset cross-attn out_proj → output=0 (neutral)")
diff --git a/run_phase1_pipeline.sh b/run_phase1_pipeline.sh
index 969460b..4504051 100644
--- a/run_phase1_pipeline.sh
+++ b/run_phase1_pipeline.sh
@@ -13,6 +13,7 @@ set -e
 
 PYTHON=/public/home/maoyaoxin/anaconda3/envs/cinebrain/bin/python
 export CUDA_HOME=/usr/local/cuda-12.4
+NGPU=${NGPU:-1}  # Number of GPUs, set via env: NGPU=4 bash run_phase1_pipeline.sh
 
 cd /public/home/maoyaoxin/xxt/SF-v1/CineBrain
 
@@ -73,7 +74,7 @@ echo "============================================"
 echo "  STAGE 1: Branch Pre-training ($S1_ITERS iters)"
 echo "============================================"
 
-$PYTHON -m torch.distributed.run --standalone --nproc_per_node=1 \
+$PYTHON -m torch.distributed.run --standalone --nproc_per_node=$NGPU \
     train_video_fmri.py \
     --base $MODEL_YAML configs/sf_v1/sf_v1_phase1_train.yaml $TMPDIR/s1_override.yaml \
     --seed 42 \
@@ -109,7 +110,7 @@ echo "============================================"
 echo "  STAGE 2: Fusion Training ($S2_ITERS iters)"
 echo "============================================"
 
-$PYTHON -m torch.distributed.run --standalone --nproc_per_node=1 \
+$PYTHON -m torch.distributed.run --standalone --nproc_per_node=$NGPU \
     train_video_fmri.py \
     --base $MODEL_YAML configs/sf_v1/sf_v1_stage2_fusion.yaml $TMPDIR/s2_override.yaml \
     --seed 42 \
@@ -143,7 +144,7 @@ echo "============================================"
 echo "  STAGE 3: Joint Fine-tuning ($S3_ITERS iters)"
 echo "============================================"
 
-$PYTHON -m torch.distributed.run --standalone --nproc_per_node=1 \
+$PYTHON -m torch.distributed.run --standalone --nproc_per_node=$NGPU \
     train_video_fmri.py \
     --base $MODEL_YAML configs/sf_v1/sf_v1_stage3_joint.yaml $TMPDIR/s3_override.yaml \
     --seed 42 \
diff --git a/sgm/modules/diffusionmodules/loss.py b/sgm/modules/diffusionmodules/loss.py
index 0c58b02..ca3ab80 100644
--- a/sgm/modules/diffusionmodules/loss.py
+++ b/sgm/modules/diffusionmodules/loss.py
@@ -188,10 +188,14 @@ class VideoDiffusionLossSF(VideoDiffusionLoss):
     - fusion: L_align + L_slow + L_fast + L_guide (no diffusion loss)
     - joint: L_diff + L_align + L_slow + L_fast + L_guide
     """
-    def __init__(self, training_stage="joint", sf_loss_config=None, lambda_sf=0.003, **kwargs):
+    def __init__(self, training_stage="joint", sf_loss_config=None, lambda_sf=0.003, lambda_router=None, lambda_router_decay_iter=50, lambda_router_decay_to=0.03, **kwargs):
         super().__init__(**kwargs)
         self.training_stage = training_stage
         self.lambda_sf = lambda_sf
+        self.lambda_router_base = lambda_sf if lambda_router is None else lambda_router
+        self.lambda_router_decay_iter = lambda_router_decay_iter
+        self.lambda_router_decay_to = lambda_router_decay_to
+        self.lambda_router = self.lambda_router_base  # Will be updated in forward
 
         from sgm.modules.diffusionmodules.sf_losses import (
             AlignmentLoss, SlowBranchLoss, FastBranchDistillLoss, GuidanceLoss
@@ -224,6 +228,11 @@ class VideoDiffusionLossSF(VideoDiffusionLoss):
             lambda_gk=cfg.get("lambda_gk", 0.5),
             lambda_gt=cfg.get("lambda_gt", 0.5),
             lambda_gm=cfg.get("lambda_gm", 0.5),
+            lambda_alpha_mot=cfg.get("lambda_alpha_mot", 0.0),
+            lambda_alpha_nonbrain=cfg.get("lambda_alpha_nonbrain", 0.0),
+            alpha_nonbrain_floor=cfg.get("alpha_nonbrain_floor", 0.35),
+            lambda_alpha_motion_margin=cfg.get("lambda_alpha_motion_margin", 0.0),
+            alpha_motion_margin=cfg.get("alpha_motion_margin", 0.10),
         )
 
         from sgm.modules.diffusionmodules.sf_losses import AuxAlignmentLoss
@@ -238,6 +247,7 @@ class VideoDiffusionLossSF(VideoDiffusionLoss):
         embedder = conditioner.embedders[0]
         slow_out = getattr(embedder, '_last_slow_out', {})
         fast_out = getattr(embedder, '_last_fast_out', {})
+        alphas = getattr(embedder, '_last_alphas', {})
 
         # Move sf_targets to device and match dtype (bf16-safe)
         targets = batch.get("sf_targets", {})
@@ -250,6 +260,7 @@ class VideoDiffusionLossSF(VideoDiffusionLoss):
 
         # Initialize loss accumulator matching input dtype (bf16)
         sf_total = input.new_tensor(0.0)
+        router_total = input.new_tensor(0.0)
 
         # Alignment loss (P1-4: pass MoCo queues for bs=1 contrastive)
         if "fmri_cls" in slow_out and "eeg_cls" in fast_out:
@@ -283,8 +294,13 @@ class VideoDiffusionLossSF(VideoDiffusionLoss):
             video_embed = targets.get("gt_keyframe_embed", None)
             text_embed = targets.get("gt_text_embed", None)
             if "z_key" in slow_out and (video_embed is not None or text_embed is not None):
-                l_guide, _ = self.guide_loss(slow_out, fast_out, targets, video_embed, text_embed)
-                sf_total = sf_total + l_guide
+                l_guide, guide_losses = self.guide_loss(
+                    slow_out, fast_out, targets, video_embed, text_embed, alphas=alphas
+                )
+                guide_nonrouter = self.guide_loss.weighted_nonrouter_total(guide_losses, ref=sf_total)
+                guide_router = self.guide_loss.weighted_router_total(guide_losses, ref=sf_total)
+                sf_total = sf_total + guide_nonrouter
+                router_total = router_total + guide_router
 
         # Diffusion loss (stage 2 fusion + stage 3 joint)
         if self.training_stage in ("fusion", "joint"):
@@ -323,15 +339,26 @@ class VideoDiffusionLossSF(VideoDiffusionLoss):
 
             diff_loss = self.get_loss(model_output, input, w)
 
-            # Fixed SF loss weight (replaces old dynamic scaling that made lambdas ineffective)
+            # Apply lambda_router decay after decay_iter (if configured)
+            global_step = batch.get("global_step", 0)
+            if hasattr(self, 'lambda_router_base') and self.lambda_router_decay_iter > 0:
+                if global_step >= self.lambda_router_decay_iter:
+                    self.lambda_router = self.lambda_router_decay_to
+                else:
+                    self.lambda_router = self.lambda_router_base
+
+            # Fixed SF loss weights. Router losses can bypass the tiny global SF scale in joint stage.
             raw_sf = sf_total.detach().float().item()
+            raw_router = router_total.detach().float().item()
             sf_total = sf_total * self.lambda_sf
+            router_total = router_total * self.lambda_router
 
-            # Store loss breakdown for logging (include raw sf_total and ratio for diagnostics)
+            # Store loss breakdown for logging (include raw totals and ratio for diagnostics)
             self._last_loss_breakdown = {
                 "debug/raw_sf_total": raw_sf,
+                "debug/raw_router_total": raw_router,
                 "debug/diff_loss": diff_loss.detach().float().mean().item(),
-                "debug/sf_diff_ratio": (raw_sf * self.lambda_sf) / (diff_loss.detach().float().mean().item() + 1e-8),
+                "debug/sf_diff_ratio": ((raw_sf * self.lambda_sf) + (raw_router * self.lambda_router)) / (diff_loss.detach().float().mean().item() + 1e-8),
             }
             if 'l_align' in locals():
                 self._last_loss_breakdown["sf/L_align"] = l_align.detach().float().item()
@@ -346,9 +373,13 @@ class VideoDiffusionLossSF(VideoDiffusionLoss):
                 self._last_loss_breakdown["sf/L_aux"] = l_aux.detach().float().item()
             if 'l_guide' in locals():
                 self._last_loss_breakdown["sf/L_guide"] = l_guide.detach().float().item()
+            if "guide_losses" in locals():
+                for gk, gv in guide_losses.items():
+                    self._last_loss_breakdown["sf/" + gk] = gv.detach().float().item()
+            self._last_loss_breakdown["sf/router_total"] = router_total.detach().float().item()
             self._last_loss_breakdown["sf/total"] = sf_total.detach().float().item()
 
-            return diff_loss + sf_total.to(diff_loss.dtype)
+            return diff_loss + sf_total.to(diff_loss.dtype) + router_total.to(diff_loss.dtype)
 
         # Store loss breakdown for logging
         self._last_loss_breakdown = {}
@@ -365,6 +396,9 @@ class VideoDiffusionLossSF(VideoDiffusionLoss):
             self._last_loss_breakdown["sf/L_aux"] = l_aux.detach().float().item()
         if 'l_guide' in locals():
             self._last_loss_breakdown["sf/L_guide"] = l_guide.detach().float().item()
+        if "guide_losses" in locals():
+            for gk, gv in guide_losses.items():
+                self._last_loss_breakdown["sf/" + gk] = gv.detach().float().item()
         self._last_loss_breakdown["sf/total"] = sf_total.detach().float().item()
 
         # For branch_pretrain stage: return SF loss only (no diffusion)
diff --git a/sgm/modules/diffusionmodules/sf_losses.py b/sgm/modules/diffusionmodules/sf_losses.py
index f4d18ed..1b3566b 100644
--- a/sgm/modules/diffusionmodules/sf_losses.py
+++ b/sgm/modules/diffusionmodules/sf_losses.py
@@ -237,13 +237,62 @@ class FastBranchDistillLoss(nn.Module):
 
 class GuidanceLoss(nn.Module):
     """Guidance consistency: head predictions should match stimulus embeddings."""
-    def __init__(self, lambda_gk=0.5, lambda_gt=0.5, lambda_gm=0.5):
+    def __init__(
+        self,
+        lambda_gk=0.5,
+        lambda_gt=0.5,
+        lambda_gm=0.5,
+        lambda_alpha_mot=0.0,
+        lambda_alpha_nonbrain=0.0,
+        alpha_nonbrain_floor=0.35,
+        lambda_alpha_motion_margin=0.0,
+        alpha_motion_margin=0.10,
+    ):
         super().__init__()
         self.lambda_gk = lambda_gk
         self.lambda_gt = lambda_gt
         self.lambda_gm = lambda_gm
-
-    def forward(self, slow_out, fast_out, targets, video_embed=None, text_embed=None):
+        self.lambda_alpha_mot = lambda_alpha_mot
+        self.lambda_alpha_nonbrain = lambda_alpha_nonbrain
+        self.alpha_nonbrain_floor = alpha_nonbrain_floor
+        self.lambda_alpha_motion_margin = lambda_alpha_motion_margin
+        self.alpha_motion_margin = alpha_motion_margin
+
+    def _weighted_total(self, losses, specs, ref=None):
+        if ref is not None:
+            total = ref.new_tensor(0.0)
+        elif losses:
+            total = next(iter(losses.values())).new_tensor(0.0)
+        else:
+            total = torch.tensor(0.0)
+        for key, weight in specs:
+            if weight > 0 and key in losses:
+                total = total + weight * losses[key]
+        return total
+
+    def weighted_nonrouter_total(self, losses, ref=None):
+        return self._weighted_total(
+            losses,
+            [
+                ("L_gk", self.lambda_gk),
+                ("L_gt", self.lambda_gt),
+                ("L_gm", self.lambda_gm),
+            ],
+            ref=ref,
+        )
+
+    def weighted_router_total(self, losses, ref=None):
+        return self._weighted_total(
+            losses,
+            [
+                ("L_alpha_mot_dyn", self.lambda_alpha_mot),
+                ("L_alpha_nonbrain", self.lambda_alpha_nonbrain),
+                ("L_alpha_motion_margin", self.lambda_alpha_motion_margin),
+            ],
+            ref=ref,
+        )
+
+    def forward(self, slow_out, fast_out, targets, video_embed=None, text_embed=None, alphas=None):
         _ref = next(iter(slow_out.values()))
         losses = {}
         total = _ref.new_tensor(0.0)
@@ -267,6 +316,40 @@ class GuidanceLoss(nn.Module):
             losses["L_gm"] = F.mse_loss(mot_energy * scale, flow_energy)
             total = total + self.lambda_gm * losses["L_gm"]
 
+        if alphas is not None:
+            alpha_mot = alphas.get("alpha_mot")
+            alpha_key = alphas.get("alpha_key")
+            alpha_txt = alphas.get("alpha_txt")
+            if self.lambda_alpha_mot > 0 and alpha_mot is not None and "gt_dyn_label_2class" in targets:
+                alpha_prob = alpha_mot.float().clamp(1e-6, 1 - 1e-6)
+                dyn_target = targets["gt_dyn_label_2class"].float().view_as(alpha_prob)
+                losses["L_alpha_mot_dyn"] = F.binary_cross_entropy(alpha_prob, dyn_target)
+            if (
+                self.lambda_alpha_nonbrain > 0
+                and alpha_key is not None
+                and alpha_txt is not None
+                and alpha_mot is not None
+            ):
+                nonbrain_mass = alpha_key + alpha_txt + alpha_mot
+                floor = nonbrain_mass.new_full(nonbrain_mass.shape, self.alpha_nonbrain_floor)
+                losses["L_alpha_nonbrain"] = F.relu(floor - nonbrain_mass).mean()
+            if (
+                self.lambda_alpha_motion_margin > 0
+                and alpha_key is not None
+                and alpha_txt is not None
+                and alpha_mot is not None
+                and "gt_dyn_label_2class" in targets
+            ):
+                semantic_mass = alpha_key + alpha_txt
+                dyn_target = targets["gt_dyn_label_2class"].float().view_as(alpha_mot)
+                margin = alpha_mot.new_full(alpha_mot.shape, self.alpha_motion_margin)
+                dynamic_penalty = F.relu(semantic_mass + margin - alpha_mot)
+                static_penalty = F.relu(alpha_mot + margin - semantic_mass)
+                losses["L_alpha_motion_margin"] = (
+                    dyn_target * dynamic_penalty + (1.0 - dyn_target) * static_penalty
+                ).mean()
+
+        total = self.weighted_nonrouter_total(losses, ref=_ref) + self.weighted_router_total(losses, ref=_ref)
         return total, losses
 
 
diff --git a/sgm/modules/encoders/gated_fusion.py b/sgm/modules/encoders/gated_fusion.py
index a82a13f..d1169a8 100644
--- a/sgm/modules/encoders/gated_fusion.py
+++ b/sgm/modules/encoders/gated_fusion.py
@@ -4,11 +4,33 @@ P2-1 redesign: Replace concat+shared_projection+take_slow with cross-attention m
 Slow tokens query fast tokens via cross-attention, allowing fast branch information
 to directly influence the output instead of being squeezed through a bottleneck.
 """
+import math
 import torch
 import torch.nn as nn
 from sgm.modules.encoders.common import CrossAttention, TransformerEncoderLayer
 
 
+def _reset_linear(layer, gain=1.0):
+    nn.init.xavier_uniform_(layer.weight, gain=gain)
+    if layer.bias is not None:
+        nn.init.zeros_(layer.bias)
+
+
+def _reset_norm(norm):
+    if getattr(norm, "weight", None) is not None:
+        nn.init.ones_(norm.weight)
+    if getattr(norm, "bias", None) is not None:
+        nn.init.zeros_(norm.bias)
+
+
+def _reset_module_tree(module, gain=1.0):
+    for sub in module.modules():
+        if isinstance(sub, nn.Linear):
+            _reset_linear(sub, gain=gain)
+        elif isinstance(sub, nn.LayerNorm):
+            _reset_norm(sub)
+
+
 class CrossModalGatedFusion(nn.Module):
     """
     v2: Cross-attention fusion.
@@ -60,25 +82,43 @@ class CrossModalGatedFusion(nn.Module):
                 nn.Linear(hidden_dim * 2, hidden_dim),
                 nn.GELU(),
                 nn.Linear(hidden_dim, num_alphas),
-                nn.Sigmoid(),
             )
-            # Initialize last linear bias to 0 → sigmoid(0)=0.5 for all alphas
-            nn.init.zeros_(self.gate_net[-2].weight)
-            nn.init.zeros_(self.gate_net[-2].bias)
+            self.reset_gate_net()
         else:
             self.register_buffer(
                 "fixed_alpha", torch.ones(num_alphas) / num_alphas
             )
 
     def reset_gate_net(self):
-        """Re-initialize gate_net to sigmoid(0)=0.5 for all channels.
-        Call after loading checkpoint to avoid inheriting saturated alpha values."""
+        """Reset gate_net to a uniform 0.25 prior under the hierarchical budget."""
         if hasattr(self, 'gate_net'):
-            nn.init.zeros_(self.gate_net[-2].weight)
-            nn.init.zeros_(self.gate_net[-2].bias)
-            # Also reset first layer to small values
-            nn.init.xavier_uniform_(self.gate_net[0].weight, gain=0.1)
-            nn.init.zeros_(self.gate_net[0].bias)
+            with torch.no_grad():
+                nn.init.xavier_uniform_(self.gate_net[0].weight, gain=0.1)
+                nn.init.zeros_(self.gate_net[0].bias)
+                nn.init.zeros_(self.gate_net[-1].weight)
+                prior_bias = torch.tensor(
+                    [
+                        math.log(0.75 / 0.25),  # nonbrain = 0.75 -> alpha_brain = 0.25
+                        math.log((1.0 / 3.0) / (2.0 / 3.0)),  # motion share inside nonbrain = 1/3
+                        0.0,  # semantic split key/txt = 0.5 / 0.5
+                        0.0,
+                    ],
+                    device=self.gate_net[-1].bias.device,
+                    dtype=self.gate_net[-1].bias.dtype,
+                )
+                self.gate_net[-1].bias.copy_(prior_bias)
+
+    def reset_fusion_state(self, full=False):
+        """Reset the gating head and optionally the whole fusion stack."""
+        self.reset_gate_net()
+        if not full:
+            return
+        _reset_linear(self.slow_proj)
+        _reset_linear(self.fast_proj)
+        for layer in self.cross_attn_layers:
+            _reset_module_tree(layer)
+        _reset_norm(self.norm)
+        _reset_linear(self.output_proj)
 
     def forward(self, slow_feat, fast_feat):
         """
@@ -115,7 +155,21 @@ class CrossModalGatedFusion(nn.Module):
             alpha_vec = self.fixed_alpha.unsqueeze(0).expand(B, -1)
         else:
             pooled = torch.cat([h_slow.mean(dim=1), h_fast.mean(dim=1)], dim=-1)  # (B, 2*hidden)
-            alpha_vec = self.gate_net(pooled)  # (B, 4)
+            alpha_logits = self.gate_net(pooled)  # (B, 4)
+            nonbrain = torch.sigmoid(alpha_logits[:, 0:1])
+            motion_share = torch.sigmoid(alpha_logits[:, 1:2])
+            semantic_logits = alpha_logits[:, 2:4]
+            semantic_split = torch.softmax(semantic_logits, dim=-1)
+            semantic_mass = nonbrain * (1.0 - motion_share)
+            alpha_vec = torch.cat(
+                [
+                    semantic_mass * semantic_split[:, 0:1],
+                    semantic_mass * semantic_split[:, 1:2],
+                    nonbrain * motion_share,
+                    1.0 - nonbrain,
+                ],
+                dim=-1,
+            )
 
         alphas = {
             "alpha_key": alpha_vec[:, 0:1],
diff --git a/sgm/modules/encoders/multi_guidance.py b/sgm/modules/encoders/multi_guidance.py
index 9209e90..5e19f1d 100644
--- a/sgm/modules/encoders/multi_guidance.py
+++ b/sgm/modules/encoders/multi_guidance.py
@@ -9,6 +9,19 @@ import torch.nn as nn
 import torch.nn.functional as F
 
 
+def _reset_linear(layer, gain=1.0):
+    nn.init.xavier_uniform_(layer.weight, gain=gain)
+    if layer.bias is not None:
+        nn.init.zeros_(layer.bias)
+
+
+def _reset_norm(norm):
+    if getattr(norm, "weight", None) is not None:
+        nn.init.ones_(norm.weight)
+    if getattr(norm, "bias", None) is not None:
+        nn.init.zeros_(norm.bias)
+
+
 class GuidanceCrossAttention(nn.Module):
     """Lightweight single-channel cross-attention: z_b queries one guidance token."""
     def __init__(self, brain_dim, num_heads=8):
@@ -26,6 +39,16 @@ class GuidanceCrossAttention(nn.Module):
         nn.init.zeros_(self.out_proj.weight)
         nn.init.zeros_(self.out_proj.bias)
 
+    def reset_outputs(self):
+        nn.init.zeros_(self.out_proj.weight)
+        nn.init.zeros_(self.out_proj.bias)
+
+    def reset_full(self):
+        _reset_linear(self.q_proj)
+        _reset_linear(self.k_proj)
+        _reset_linear(self.v_proj)
+        self.reset_outputs()
+
     def forward(self, query, kv):
         """query: (B, S, D), kv: (B, 1, D) → (B, S, D)"""
         B, Sq, D = query.shape
@@ -97,20 +120,37 @@ class MultiGuidanceAdapter(nn.Module):
         for name in ['key_attn', 'txt_attn', 'mot_attn']:
             attn = getattr(self, name, None)
             if attn is not None:
-                nn.init.zeros_(attn.out_proj.weight)
-                nn.init.zeros_(attn.out_proj.bias)
+                attn.reset_outputs()
+
+    def reset_guidance_state(self, full=False):
+        """Reset guidance outputs and optionally all guidance projections."""
+        self.reset_guidance_outputs()
+        if not full:
+            return
+        for name in ['key_proj', 'txt_proj', 'mot_proj', 'temporal_proj', 'temporal_gate']:
+            layer = getattr(self, name, None)
+            if layer is not None:
+                _reset_linear(layer)
+        for name in ['key_attn', 'txt_attn', 'mot_attn']:
+            attn = getattr(self, name, None)
+            if attn is not None:
+                attn.reset_full()
+        _reset_norm(self.norm)
+        for layer in self.out_proj:
+            if isinstance(layer, nn.LayerNorm):
+                _reset_norm(layer)
+            elif isinstance(layer, nn.Linear):
+                _reset_linear(layer)
 
     def forward(self, z_b, alphas, slow_out, fast_out):
         """
-        Each guidance channel: proj → cross-attn(z_b, guidance) → alpha * result
-        All channels added to z_b as residuals.
+        z_b is the always-on base path.
+        Each guidance channel adds an alpha-weighted residual on top of z_b.
+        alpha_brain remains a slack/null channel in the softmax budget, but it
+        no longer rescales z_b directly.
         """
-        # Differentiable alpha floor: map [0,1] → [0.05, 0.95]
-        # Unlike clamp, gradient always flows (0.9) so gate_net can learn.
-        alphas = {k: v * 0.9 + 0.05 for k, v in alphas.items()}
-
-        context = z_b.clone()
         q = self.norm(z_b)
+        context = z_b
 
         if self.use_keyframe_guidance and "z_key" in slow_out:
             g_key = self.key_proj(slow_out["z_key"]).unsqueeze(1)  # (B, 1, D)
@@ -123,14 +163,22 @@ class MultiGuidanceAdapter(nn.Module):
         if self.use_motion_guidance:
             eeg_feat = fast_out.get("eeg_pooled_proj", None)
             if eeg_feat is not None:
-                g_mot = self.mot_proj(eeg_feat).unsqueeze(1)
-                if self.use_temporal_guidance and "global_dyn_token" in fast_out:
-                    g_temporal = self.temporal_proj(fast_out["global_dyn_token"])
-                    gate = torch.sigmoid(self.temporal_gate(fast_out["global_dyn_token"]))
-                    g_mot = g_mot + gate.unsqueeze(-1) * g_temporal.unsqueeze(1)
+                mot_tokens = [self.mot_proj(eeg_feat).unsqueeze(1)]
+                if self.use_temporal_guidance:
+                    gate = None
+                    if "global_dyn_token" in fast_out:
+                        gate = torch.sigmoid(self.temporal_gate(fast_out["global_dyn_token"])).unsqueeze(-1)
+                    if "temporal_tokens" in fast_out:
+                        g_temporal = self.temporal_proj(fast_out["temporal_tokens"])
+                        if gate is not None:
+                            g_temporal = gate * g_temporal
+                        mot_tokens.append(g_temporal)
+                    elif "global_dyn_token" in fast_out:
+                        g_temporal = self.temporal_proj(fast_out["global_dyn_token"]).unsqueeze(1)
+                        if gate is not None:
+                            g_temporal = gate * g_temporal
+                        mot_tokens.append(g_temporal)
+                g_mot = torch.cat(mot_tokens, dim=1)
                 context = context + alphas["alpha_mot"].unsqueeze(-1) * self.mot_attn(q, g_mot)
 
-        if self.use_brain_latent_guidance:
-            context = context + alphas["alpha_brain"].unsqueeze(-1) * z_b
-
         return self.out_proj(context)
diff --git a/tools/evaluate_p1.py b/tools/evaluate_p1.py
index afc7eff..31411d0 100644
--- a/tools/evaluate_p1.py
+++ b/tools/evaluate_p1.py
@@ -45,6 +45,11 @@ def build_embedder(model_config_path, device):
     with open(model_config_path) as f:
         cfg = yaml.safe_load(f)
     emb_cfg = cfg["model"]["conditioner_config"]["params"]["emb_models"][0]["params"]
+    # Mirror the training-time override path in diffusion_video_brain.py so eval
+    # uses the same embedder shape as the checkpoint-producing run.
+    for key in ["sparse_attn_drop", "flow_codebook_k"]:
+        if key in cfg.get("model", {}):
+            emb_cfg[key] = cfg["model"][key]
     embedder = SFBrainEmbedder(**emb_cfg)
     embedder.to(device).to(torch.bfloat16)
     embedder.mode = "infer"  # skip CLIP loss
